const App = () => {
        const groom = "hammad"
        const bride = "hassan"
        return (
            <div className="w-full bg-blue-800 h-screen flex items-center justify-center">
                <h1 className="text-white font-sans font-bold capitalize">{groom} weds {bride}</h1>
            </div>
        )
    }

export default App

